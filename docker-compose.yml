services:
  autoarchitector:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: bash -lc "pip install -r requirements.txt && python main.py --config config/sources.yaml --output output/"
